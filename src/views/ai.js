// AI View Module (stub modular — app de produção usa script.js)

export function initAi(container) {
  container.innerHTML = `
    <div class="panel">
      <h2>DocSpace IA</h2>
      <p class="message">Módulo modular em construção. Use a aplicação principal (script.js) para o chat de IA.</p>
    </div>
  `;
}

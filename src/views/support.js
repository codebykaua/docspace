// Support View Module (stub modular — app de produção usa script.js)

export function initSupport(container) {
  container.innerHTML = `
    <div class="panel">
      <h2>Atendimento</h2>
      <p class="message">Módulo modular em construção. Use a aplicação principal (script.js) para suporte completo.</p>
    </div>
  `;
}
